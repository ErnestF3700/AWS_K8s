apiVersiin: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: flask-app-role
  namespace: dev
rules:
- apiGroups: [""]
  resources:
  - pods
  - services
  - configmaps
  verbs:
  - get
  - list
  - watch 