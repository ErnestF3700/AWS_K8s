apiVersion: v1
kind: PersistentVolumeClaim

metadata:
  name: flask-app-storage
  namespace: dev

spec:
  accessModes:
    - ReadWriteOnce

  storageClassName: ebs-gp3

  resources:
    requests:
      storage: 5Gi