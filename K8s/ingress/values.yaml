controller:
  replicaCount: 2
  service: 
    type: LoadBalancer
    metrics:
      enabled: true

  ingressClassResource:
    name: nginx
    enabled: true
    default: true

  admissionWebhooks:
    enabled: true
defaultBackend:
  enabled: true